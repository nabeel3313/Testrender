services:
  - type: web
    name: mentorai-showcase
    runtime: python
    buildCommand: "pip install -r requirements_showcase.txt"
    startCommand: "gunicorn -w 2 --bind 0.0.0.0:$PORT app_showcase:app"
    plan: free
    healthCheckPath: /
